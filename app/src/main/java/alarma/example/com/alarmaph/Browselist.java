package alarma.example.com.alarmaph;

/**
 * Created by Carlo on 1/1/2018.
 */

public class Browselist{

    /*private final Activity context;
    private final String[] bname;
    private final String[] bnum;
    private final String[] bloc;

    public Browselist(Activity context, String[] bname, String[] bnum, String[] bloc){
        super(context,R.layout.list_browse, bname);

        this.context = context;
        this.bname = bname;
        this.bnum = bnum;
        this.bloc = bloc;

    }

    @Override
    public View getView(int position, View view, ViewGroup parent) {
        LayoutInflater inflater = context.getLayoutInflater();
        View rowView= inflater.inflate(R.layout.list_browse, null, true);

        TextView txtbName = (TextView) rowView.findViewById(R.id.bn);
        TextView txtbNum = (TextView) rowView.findViewById(R.id.bnum);
        TextView txtbLoc = (TextView) rowView.findViewById(R.id.bl);

        txtbName.setText(bname[position]);
        txtbNum.setText(bnum[position]);
        txtbLoc.setText(bloc[position]);

        return rowView;
    }*/
}
